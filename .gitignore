# Binaries
/bin/

# Go build/test artifacts
*.test
*.out
/vendor/

# Environment files
.env
.env.*

# OS files
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/*
!.vscode/launch.json
.idea/

# Claude Code internal storage
.claude/

# Dev container lock file (no features used, nothing to pin)
.devcontainer/devcontainer-lock.json

# folder to store temporary files (AI analysis, dev todos etc.)
/temp-docs/

# Personal/local scripts (e.g. release.ps1) — not part of the committed
# build (that's build.sh / build-container.ps1 at the repo root).
/scripts/

# Archives staged by scripts/release.ps1
/release-out/